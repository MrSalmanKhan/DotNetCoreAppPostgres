@{
    ViewData["Title"] = "Signed out";
}

<div class="d-flex justify-content-center align-items-center vh-100 bg-light">
    <div class="card shadow border-0 rounded-3" style="max-width: 540px;">
        <div class="card-body text-center p-5">
            <h2>Signed out</h2>
            <p class="text-muted">You have been signed out successfully.</p>

            <a asp-controller="Home" asp-action="Index" class="btn btn-outline-primary mt-3">Return home</a>
        </div>
    </div>
</div>
